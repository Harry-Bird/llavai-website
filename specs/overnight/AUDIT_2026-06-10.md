# Llavai full-stack audit — overnight 2026-06-10/11

Five parallel read-only audits: frontend (headless Chrome, all 15 pages), Supabase (live
schema vs frontend calls + advisors + logs), n8n (all 16 workflows + 289 executions),
Stripe (products/prices/subs/webhooks), spec-vs-built gap analysis. Every BROKEN finding
was re-verified before inclusion. Nothing was modified during the audit.

## ❌ BROKEN

**B1 · The 3-day trial is NOT live — published W4 checkout is an old version.**
n8n `7v8gjHd91PtNBSa1` "Stripe — Create Checkout Session": the draft (4766700d…) has
`trial_period_days=3` + plan metadata; the **published** version (611e4527…) has neither.
Real checkouts charge €19 immediately, no trial, no plan stamping (plan survives only via
W3's price-ID fallback). Spec marks Phase 1 "✅ DONE" — it isn't, classic draft-vs-publish
gotcha. → **Fix: publish the W4 draft + one test checkout** (NEEDS HARRY — no publishes tonight).

**B2 · `alert()` in the checkout path, English-only** — account.html:857 + :864 inside
`subscribe()`. Violates the friendly-trilingual-errors rule at the worst possible moment
(payment). → Fixable tonight (inline trilingual error via existing pattern).

## 🕳 MISSING (designed or obviously needed, not built)

**M1 · Business-hours call gate (owner-requested tonight).** Confirmed: W1 has NO
time/weekday gate — chain is Classify → tier gate → dedup claim → scrape → score →
`Should Call?` → **`Trigger Retell Call` fires any hour, any day**. A 03:00 Pro alert
dials the agency at 03:00. Note: a naive "skip after hours" would permanently consume the
dedup slot — must queue, not skip. → Tonight: `call_queue` migration + W1 draft gate +
drain workflow (draft, unpublished).

**M2 · No error handling on the entire new n8n stack.** W1, W2, W3, W4, Feed Backfill,
Profile Creation have no `errorWorkflow` (the existing "Error Handling" `WUv9QtLxzVWzIhJT`
is wired only to legacy flows). Worst case (W1): Retell/API failure strands the
`call_attempts` row in `queued`/`calling` forever, dedup blocks retry, nobody alerted.
Failed scrapes are likewise terminal (skip consumes the attempt). → Draft fixes tonight,
publish in morning.

**M3 · W5 Pro apply→approve flow** — last open Phase-3 item. No way for a client to apply
for Pro or for Harry to approve + send the Pro checkout link.

**M4 · Stripe sync unverified end-to-end.** W3 has **0 executions ever** despite 3 checkout
sessions created; webhook endpoint registration on the Stripe side unverifiable via MCP.
Also Stripe MCP mode shows **0 subscriptions of any status** — possible test/live mode
split. The 2026-06-09 "end-to-end verified" sub isn't visible in this mode.

**M5 · Phase 4 not started** — 6 of 7 Google Sheets workflows still ACTIVE, incl.
Profile Creation (live lead-capture path; lost a real lead 06-04 to a Sheets column error)
and confirm_viewing logger. W6 document retention (GDPR) unbuilt; SETUP.md compliance
checklist (DPA, consent checkbox, retention, privacy policy, custom SMTP) all open.

**M6 · Repo schema files badly trail the live DB** — supabase/schema.sql etc. are missing
3 tables, 2 views, 4 functions, ~40 columns, 8 policies that exist live (everything since
2026-06-09 was applied via dashboard migrations only). supabase-config.js's "security is
enforced by RLS in supabase/schema.sql" claim is stale. Also schema.sql still documents the
OLD "no user writes to viewings" rule that the live self-manage policies deliberately
replaced — next person could "fix" it backwards.

## ⚠️ RISKY

**R1 · Unauthenticated Retell Agent Manager webhook (ACTIVE).** `FjW1V5CLHHUTxflI`,
guessable path `/webhook/retell-agent-manager`, proxies get/patch/create/**publish** on
Julia's agent + LLM using the stored API key. Anyone guessing the path can rewrite the
prompt that talks to estate agents. Superseded by the secret-path W2 Setup workflow.
→ **Unpublish/archive in the morning** (top security item).

**R2 · Start Call v2.5 still ACTIVE** on its own CloudMailin path — no tier gate, same
agent + number as W1. Double-call race + non-Pro calls possible if CloudMailin still
(or ever again) targets it. Last fired 06-05. → Confirm CloudMailin target, then unpublish.

**R3 · Spot Sheets→Supabase sync ACTIVE every 15 min** (~250 runs/wk, dominates the log,
hammers `profiles` with ~24 identical lookups per burst — also visible in Supabase API
logs). Uses legacy >70 gate and its upsert can **resurrect listings the user dismissed**
(forces status back to 'new'). → Deactivate (Phase-4) or drop `status` from upsert.

**R4 · W3 sync hardcodes `status:'active'` on checkout.session.completed** — event-order
race can record a trialing sub as active ('trial' tier shows as 'essential'); also reads
`current_period_end` from a field newer Stripe API versions moved. Matters the moment B1
ships the trial. → Fix in same W3 pass.

**R5 · Stray active €0 price** `price_1TgL9eF7TyaJ4FzifrGNTqZ1` ("3 DAY TRIAL") on the
Essential product — any sub landing on it gets permanent free Essential via W3's fallback.
→ Archive in Stripe dashboard (write op — morning).

**R6 · W4 checkout webhook accepts junk** — empty-body curl produced a Stripe 400 +
caller-facing 500 (execution 2856); `return_to` unvalidated (open-redirect-ish into
success_url). → Validation node in W4 draft.

**R7 · `teaser_listings` is SECURITY DEFINER** (advisor ERROR) — intentional (free-tier
teaser needs cross-user read; columns are anonymised) but any future column added leaks to
all signed-in users. → Document + comment; review on every change.

**R8 · Anon can EXECUTE current_tier()/is_pro()/has_active_subscription()** (advisor WARN)
— returns false/'free' so impact nil, but needless surface. → Revoke migration drafted.

## ✓ HEALTHY (verified, no action)

Frontend: 0 console/page errors on all 15 pages; 0 broken links; 0 mobile overflow at
320/344/360/393/430 (mobile UA); data-lang trilingual structurally complete everywhere.
Supabase: every table/RPC/bucket the frontend calls exists with the right shape; RLS on
all 9 tables; design rules hold (subscriptions select-only, property_cache no API access).
n8n: W1 logic matches spec exactly incl. atomic dedup; W2 post-call matches its design
§4–6 exactly, no drift, 3/3 green. Stripe: both prices exist at right amounts; W3 price→plan
map correct; no Stripe keys or price IDs in the repo.

## MINOR (queue when convenient)

- Enter-submit gaps: profile.html wizard has no `<form>` at all (55 controls); account.html
  add-viewing + preference fields outside forms.
- 4 English-only placeholders never swapped by setLang() (profile.html:383/440/510,
  account.html:465).
- Canonicals/og:url/sitemap point at apex `llavai.com` (307 hop) + blog trailing slashes
  (extra 308). Move to `https://www.llavai.com/...`.
- `undoDismiss()` can write null into NOT NULL `listings.status` (currently unreachable) —
  fall back to 'new'.
- Perf lints: `auth_rls_initplan` on all 19 policies (`(select auth.uid())` rewrite),
  unindexed FK `viewings.listing_id`.
- Stray empty storage bucket `"name documents"` (delete — write op, morning).
- STRIPE.md references a nonexistent test price; predates Trial/Pro model.
- W4 allowedOrigins still lists a Vercel preview URL; Error Handling subject typo "[Lavai".
- W1 `Cache Property` sends literal `undefined` body on skip paths (masked 400, noisy).
- Feed Backfill is ACTIVE every 6h (spec says inactive) but runs in 0.5s — likely
  `feed_search_clients` returns no rows; check view population.
- Leaked-password protection off (magic-link only today — enable anyway).
- Profile Creation draft drift (unpublished onError fix).

## Full inventories
- n8n: 16 workflows; drift on W4 (the trial!), Profile Creation, Retell Agent Manager.
  Sheets dependency map + execution table: see n8n agent report (agent a2ce7f09d48fd8611).
- Supabase: 9 tables (RLS on), 2 views, 7 functions, 10 applied migrations (agent a64a6b2384e5128cf).
