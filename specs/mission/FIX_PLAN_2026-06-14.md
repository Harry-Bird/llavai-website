# Fix execution plan — 2026-06-14 (post-audit)

Status key: ✅ STAGED (done locally, reviewable) · 🔒 NEEDS GO (gated production write) · 👤 OWNER-ONLY.

## ✅ STAGED — ready to apply (local, reversible)
1. **`supabase/migrations/20260614_w8_01_rls_hardening.sql`** — RLS `(select auth.uid())` across 20 policies
   (perf at scale), `REVOKE EXECUTE` on the two `notify_*` trigger fns (security), index `call_queue.listing_id`
   + a partial feed index. Behaviour-preserving.
2. **`supabase/migrations/20260614_w8_02_current_tier_allowance.sql`** — normalise legacy `plan='spot'`→`essential`,
   make `current_tier()` an explicit allowlist, `call_allowance` default 300→0.
3. **`supabase/migrations/20260614_w8_03_viewing_booked_notify.sql`** — in-app "Julia booked you a viewing"
   message (trigger on `viewings`→booked, source=julia). No email/consent needed. (C3 partial.)
4. **`account.html`** feed query — `select('*')` → explicit 16 columns + `.limit(60)` (H5; ~8× egress cut).
5. **`profile.html`** — `idealista_url` is now `required` (C2 root cause: 0/6 users had it set → empty feed).
   ⚠️ conversion tradeoff — watch profile-completion rate; soften with a "how to get your alert URL" helper if it dips.

## 🔒 NEEDS GO — gated production writes (your authorization)
- **Apply the 3 migrations** to live Supabase (`apply_migration`, or paste into the SQL editor).
- **Deploy the frontend** — `git commit && git push origin main` (auto-deploys to www.llavai.com).
- **Archive 12 dead n8n workflows**: `rlv02UB1RHNnQl4i, 0hqUPqF8YsxJP1Et, xpSI4mowbRPnjy03, 7v8gjHd91PtNBSa1,
  nQGk9hfDb4L6VVnx, WumvVn7fOJSgF9OJ, CcCxPBYVkcNUt2cF, 535Pb7vqeo14xY4n, 4g0N2b6hfXo4u8iD, GOo6JRVsG7oJOL2x,
  GNdoH1JSk2j1WuXF, FjW1V5CLHHUTxflI` (reversible).
- **Profile Creation (`XQU6SaN7`) Sheets node** — strip the `Save to Client_Profiles` Google Sheets node
  (keep the ops sign-up email); closes the undisclosed contact-data-to-Sheets path. Needs a workflow rebuild+republish.

## 👤 OWNER-ONLY — I can't do these (billing / legal / physical / live-billing risk)
- **C1 · Upgrade Supabase off Free** (~$25/mo, dashboard) → daily backups + PITR + egress headroom. Top priority.
- **H1 · W3 Stripe sync** — change the Upsert's `current_period_end` source from top-level to
  `{{ $json.data.object.items.data[0].current_period_end }}` (fallback to top-level), republish. *I deliberately
  will not MCP-rebuild W3 — it's a live Stripe Trigger and the prior note says hand-edit only.* Latent (0 Pro subs).
- **H2 · One real end-to-end Julia smoke test** (forward a live Idealista alert for a paying, verified, url-set user
  in business hours; confirm Retell dials + W2 writes a completed call_attempt + julia viewing). Repeat after-hours.
- **H3 · Art. 50 AI disclosure** — confirm/insert in the published Retell prompt (statutory 2026-08-02).
- **One real €12 pack purchase + one Pro checkout** as live-payment gates (refund after).
- **`audio/julia-intro.mp3`** — record + drop the file (the player already self-hides when absent, so no UI breakage).
